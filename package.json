{
  "name": "process-image-upload",
  "version": "1.0.0",
  "description": "A Lambda function to process image uploads and update images.json in S3",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1626.0"
  }
}
